# Iconify (статичные SVG) — ключ не нужен, публичный API.

# LottieFiles (анимированные иконки) — бесплатный ключ на developers.lottiefiles.com.
# Интеграция не проверена на живом API из этой сборки (см. src/clients/lottiefiles.ts) —
# первый реальный вызов может потребовать поправить эндпоинт/формат авторизации там же.
LOTTIEFILES_API_KEY=
