{
  "name": "icon-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP-сервер для поиска и получения иконок (Iconify) и Lottie-анимаций (LottieFiles) — только легитимные бесплатные официальные API, без скрейпинга.",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@iconify-json/lucide": "^1.2.124",
    "@iconify/utils": "^3.1.4",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  }
}
