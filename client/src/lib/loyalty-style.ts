import type { BonusLevel } from '@simba/shared'

/**
 * Оформление ступеней лояльности: шкала «нагрева» от холодного к яркому.
 *
 * Живёт на клиенте, а не в общем модуле: `@simba/shared` отвечает за пороги и
 * названия, оформление к ним не относится — админка и сервер красят иначе.
 * Но в ОДНОМ месте, а не в трёх: корзина, профиль и страница бонусов рисуют
 * одну и ту же лестницу, и три копии разъехались бы при первой же правке.
 *
 * Пары проверены по контрасту (WCAG AA, порог 4.5:1):
 *   navy-700 на navy-100    9.53
 *   amber-800 на amber-200  5.75
 *   navy-900 на amber-500   6.39
 * Белый текст на amber-500 даёт 2.45 и здесь запрещён — MASTER.md называет
 * amber-500 «только фон/заливка», и это тот самый случай.
 */
export const LOYALTY_STYLE: Record<BonusLevel, string> = {
  newcomer: 'bg-navy-100 text-navy-700',
  active: 'bg-amber-200 text-amber-800',
  premium: 'bg-amber-500 text-navy-900',
}

/**
 * Отметка текущей ступени. Сам нагрев показывается ВСЕГДА, на всех трёх
 * пилюлях: владелец просил, чтобы у каждой стадии был свой градус. Приглушать
 * чужие ступени нельзя — тогда у не вошедшего покупателя шкала пропадает вовсе,
 * и лестницы, по которой стоит подниматься, не видно.
 */
export const LOYALTY_CURRENT_MARK = 'font-semibold ring-2 ring-offset-1 ring-navy-300'
