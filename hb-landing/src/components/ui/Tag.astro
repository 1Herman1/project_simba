---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`inline-flex px-3 py-1.5 bg-muted text-muted-foreground text-sm rounded-lg font-medium ${className}`}>
  <slot />
</span>
