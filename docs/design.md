# Дизайн-контекст Симбы

Источник истины по визуальному качеству интерфейса. Читать перед любой
UI-задачей (компонент, страница, лендинг, письмо). Идея — не давать ИИ
генерировать «типовой красивый» интерфейс, а держать характер бренда и
список того, чего делать нельзя.

Аналог: этот файл — как `DESIGN.md` из фреймворка Impeccable, адаптированный
под Симбу и наш стек (React 18 + Vite + Tailwind).

---

## Продукт

- **Что:** интернет-магазин зоотоваров (simbazoo.ru)
- **Аудитория:** владельцы кошек, собак и мелких питомцев — люди, а не гики.
  Заходят с телефона, часто на бегу, покупают корм/наполнитель повторно.
- **Задача интерфейса:** быстро найти нужный товар, легко повторить заказ,
  не запутаться в бонусах (Сибакоины).
- **Тон:** тёплый, живой, заботливый — про питомцев, не про SaaS-дашборд.
  Дружелюбно, но без сюсюканья и без клоунады.

## Характер бренда

- Тёплый и человечный, а не «корпоративный чистый»
- Ощущение заботы о питомце, доверие
- Простота: Гермес — не разработчик, интерфейс должен быть понятен без обучения
- Мобайл-фёрст: большинство заходов с телефона

---

## Регуляторы дизайна (dials)

Три параметра 1–10 для синтеза UI. Базовая точка Симбы: **DESIGN_VARIANCE=4, MOTION_INTENSITY=3, VISUAL_DENSITY=5**. Менять только осознанно и вслух.

| Регулятор | 1–3 | 4–7 | 8–10 |
|-----------|-----|-----|------|
| **DESIGN_VARIANCE** (симметрия→асимметрия) | Предсказуемая центрированная сетка, повторяемые блоки | Смещённые layout, разные пропорции, асимметрия | Masonry, дробные сетки, много воздуха, no grid |
| **MOTION_INTENSITY** (статика→кино) | Только :hover/:active, без авто-анимаций | Плавные CSS-переходы, staggered load-in (transform/opacity), гейт «надо ли» | Scroll-driven, parallax, inertial scroll, постоянное движение |
| **VISUAL_DENSITY** (галерея→кокпит) | Много воздуха (py-32+), разреженный layout | Стандарт (py-16..24), сбалансированный | Плотно, моно-цифры, 1px разделители, никакого воздуха |

---

## Anti-references (так НЕ делаем)

Главное правило Impeccable: отрицательные примеры сильнее положительных.
Если генерируешь UI и ловишь себя на любом пункте ниже — переделывай.

### Типографика
- ❌ Шрифт `Inter` / `Arial` / системный по умолчанию как «главный» бренд-шрифт
- ❌ Всё одним весом и размером — нет иерархии
- ❌ Строки длиннее ~70–75 символов в блоках текста

### Цвет
- ❌ Фиолетово-синий градиент (`from-purple-500 to-blue-500`) — маркер AI-slop
- ❌ Серый текст на цветном фоне (плохой контраст, < 4.5:1)
- ❌ Чистый чёрный `#000` для текста и чистый серый без оттенка бренда
- ❌ Тёмные «глоу»-свечения вокруг блоков

### Компоненты и layout
- ❌ Карточка внутри карточки внутри карточки (вложенные рамки)
- ❌ Обернуть вообще всё в карточку «чтобы красиво»
- ❌ Ряды одинаковых иконка-тайлов как единственный способ показать фичи
- ❌ Боковые цветные полоски-бордеры у блоков (side-tab borders)
- ❌ Тесный padding, элементы впритык друг к другу

### Анимация
- ❌ `bounce` / `elastic` easing — ощущается устаревшим
- ❌ Анимация ради анимации, без смысла и без учёта `prefers-reduced-motion`

### Доступность (a11y)
- ❌ Тач-таргеты меньше 44×44px (пальцем на телефоне не попасть)
- ❌ Пропущенные уровни заголовков (h1 → h3 без h2)
- ❌ Кнопки/иконки без доступного текста (`aria-label`)
- ❌ Плейсхолдер вместо `<label>` у полей формы

---

## Что делаем вместо (позитивные ориентиры)

- **Шрифт:** осмысленная пара — характерный для заголовков + читаемый гротеск
  для текста. Задать в `tailwind.config` через `fontFamily`, не оставлять дефолт.
- **Цвет:** палитра от бренда Симбы (тёплые акценты), а не стоковый фиолетовый.
  Ограниченная палитра: 1 акцент + нейтрали + семантика (успех/ошибка).
- **Контраст:** текст ≥ 4.5:1, крупный текст ≥ 3:1.
- **Иерархия:** размер, вес и цвет работают вместе — видно, что главное на экране.
- **Пространство:** воздух между блоками, консистентная шкала отступов Tailwind.
- **Мобайл:** проектировать с мобильного, тач-таргеты ≥ 44px, ключевое действие
  доступно большим пальцем.
- **Движение:** короткие ease-out переходы (150–250ms), уважать
  `prefers-reduced-motion`.

---

## Домены Симбы, важные для UI

- **Цены** хранятся в копейках (`249900` = 2499 ₽) — форматировать при выводе,
  не показывать копейки как есть.
- **Сибакоины (бонусы):** объяснять понятно — 5% от заказа, 1 сибакоин = 1 ₽.
  Уровни: Новичок 0–999 / Активный 1000–4999 / Премиум 5000+. Показывать прогресс
  к следующему уровню, а не голое число.
- **Пустые состояния:** пустая корзина / нет заказов / нет избранного — не белый
  экран, а понятный next step («Загляните в каталог»).

---

## Как использовать

- Перед UI-задачей — свериться с этим файлом (особенно с anti-references).
- Для ревью визуального качества — агент `design-reviewer`.
- Для финального прохода по готовому UI — команда `/polish`.
