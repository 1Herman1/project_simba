# Дизайн-система Симбы — MASTER

**Источник истины по токенам проекта.** Читать перед написанием любого UI-кода.

> **Правило разрешения конфликтов:** если для страницы существует
> `pages/<страница>.md` — правила из неё **переопределяют** этот файл.
> Всё, что там не переопределено, берётся отсюда.

Генерируется/обновляется командой `/design-system`. Не перезаписывать целиком
без явного согласия — правки вносить точечно.

Бренд и тон — `docs/projects/simba/brand.md`. Универсальные принципы —
`docs/core/design-principles.md`.

---

## Профиль

- **Домен:** E-commerce (общий) + оттенок Pet — `docs/core/design-profiles.md`
- **Dials:** DESIGN_VARIANCE=4, MOTION_INTENSITY=3, VISUAL_DENSITY=5
- **Паттерн:** Feature-Rich Showcase (каталог-сетка, карточки с глубиной)
- **Платформа:** мобайл-фёрст, React 18 + Vite + Tailwind

---

## Палитра

Роли задаются **парами «цвет + on-цвет»** — так контраст гарантирован структурно.

| Роль | Токен | Hex | Контраст |
|---|---|---|---|
| background (страница) | `blue-50` | `#F1F1F1` | — (решение владельца: нейтральный фон) |
| foreground | `navy-900` | `#000000` | 19.4:1 ✅ (решение владельца: чистый чёрный) |
| muted-foreground (вторичный текст) | `navy-500` | `#4A5C7A` | 6.4:1 ✅ |
| disabled / декор (**не для текста**) | `navy-300` | `#8FA8C0` | 2.6:1 ❌ |
| border | `line` | `#E4EBF7` | — |
| card | `white` + `border-line` | `#FFFFFF` | — |
| **CTA (чёрная кнопка, фон под белым текстом)** | `ink` | `#232323` | on: `white` 15.2:1 ✅ |
| CTA hover | `ink-hover` | `#000000` | on: `white` 21:1 ✅ |
| primary (синие ссылки, текст, иконки) | `primary` | `#3A6FE0` | on: `white` 4.63:1 ✅ |
| primary hover + **любой синий текст/ссылка** | `primary-hover` | `#2E5DC8` | on: `white` 5.97:1 ✅ |
| primary deep (подложка кнопки при ховере — свечение у курсора на ней читается глубже; в покое кнопка плоская `primary`) | `primary-deep` | `#1E458F` | on: `white` 9.3:1 ✅ |
| primary soft (**только нетекст**: иконки, рамка выбранного, фокус) | `primary-soft` | `#4C82F7` | 3.59:1 — под текстом ❌ |
| primary tint (подложка инфо-блоков, раскрытый FAQ) | `primary-tint` | `#F2F6FE` | — |
| accent (бренд, Scoins) — **только фон/заливка** | `amber-500` | `#E8921A` | on: `white` 2.45:1 ❌ как текст запрещён |
| accent-text (рейтинг «4,9», звезда, оранжевый текст) | `amber-600` | `#A66200` | on: `white` 4.81:1 ✅ |
| accent-soft | `amber-300` | `#FFD580` | on: `navy-900` ✅ |
| success | `success` | `#1E7B4D` | on: `white` 5.29:1 ✅ |
| success tint (подложка под галочкой подтверждения) | `success-tint` | `#E4F4EB` | под `success` 4.63:1 ✅ |
| destructive | `#C0392B` | — | on: `white` ✅ |

**Синий текст — только `primary-hover` `#2E5DC8`.** `primary` и `primary-soft`
как цвет текста не проходят AA ни на белом, ни на `blue-50`. `primary-soft`
живёт на иконках, рамках и фокус-ринге, где порог 3:1.

**Цифра в кружке шага и текст пилюли «БЕСПЛАТНО» — это текст:** такие подложки
красить в `primary`, не в `primary-soft`.

### Полная шкала navy (обязательно к объявлению)

В `client/tailwind.config.ts` объявлены только `300/500/700/900`, а код использует
также `100/200/400/600` — эти классы **не генерируются** и молча не применяются,
из-за чего вторичный текст наследует `navy-900` и иерархия рушится.

```ts
navy: {
  100: '#E4EBF7',  // границы, разделители (= line)
  200: '#C4D3E0',  // фон бейджей
  300: '#8FA8C0',  // декор, зачёркнутая цена — НЕ смысловой текст
  400: '#6B85A0',  // иконки, крупный текст (3.5:1) — НЕ body
  500: '#4A5C7A',  // вторичный текст (6.4:1) ✅
  600: '#3A4B66',
  700: '#2A3A56',
  800: '#1F2E48',
  900: '#000000',  // основной текст (решение владельца: чистый чёрный)
}
```

**Правило:** смысловой текст — только `navy-500` и темнее. `navy-300/400` —
декор, иконки, зачёркнутые старые цены. На подложке `primary-tint` `navy-300`
и `navy-400` запрещены даже как декор под текстом — только `navy-500` и темнее.

### Шкалы blue и amber (как в `client/tailwind.config.ts`)

```ts
blue:  { 50: '#F1F1F1', 100: '#D6EAFE', 200: '#A4D4FC',
         300: '#6BB8F5', 400: '#3A9AE8', 500: '#1A3A5C' }
amber: { 50: '#FFF8E8', 100: '#FFEDC4', 200: '#FFD9A0', 300: '#FFD580',
         400: '#FFB347', 500: '#E8921A', 600: '#A66200', 800: '#7A4700' }
```

`blue-200` — легаси-пастель: прогресс-бары и декор. **Как фон CTA запрещена** —
действие красится в `primary`. `blue-500` `#1A3A5C` — тёмный, это цвет текста,
не заливки; не путать с `primary-soft`.

**Правило: hex в компонентах запрещён** — использовать классы Tailwind
(`text-navy-500`), а не `style={{ color: '#4A6A8A' }}`. Хардкод приводит к
дрейфу палитры: в коде уже встречается `#4A6A8C` вместо `#4A6A8A` — отличие в
один символ, глазом незаметно, но это другой цвет вне системы.
`design-lint.mjs` помечает такие места как WARNING.

---

## Типографика

```ts
fontFamily: {
  sans: ['Montserrat', 'system-ui', 'sans-serif'],
  display: ['Montserrat', 'system-ui', 'sans-serif'],
}
```

- **Семейство:** Montserrat (кириллица ✅). Решение владельца: заголовки —
  semibold (600, утилита font-bold переопределена на 600), основной текст —
  light (300); мельче 14px — regular (400), чтобы держать читаемость.
- **Подключение:** только `<link>` в `index.html` с `display=swap` +
  `preconnect` к `fonts.googleapis.com` **и** `fonts.gstatic.com`.
  `@import` в CSS — запрещён (блокирует рендер).
- **Шкала:** 12 / 14 / 16 / 18 / 24 / 32 / 40
- **Body на мобиле:** ≥ 16px (иначе iOS авто-зумит поля)
- **Line-height:** 1.5–1.75 для текста
- **Длина строки:** `max-w-prose` для абзацев (65–75 символов)
- **Цены и таблицы:** `tabular-nums` — иначе цифры прыгают

---

## Шкала отступов (VISUAL_DENSITY=5, стандарт)

| Токен | Значение | Где |
|---|---|---|
| xs | `gap-1` / 4px | внутри бейджа |
| sm | `gap-2` / 8px | между иконкой и текстом |
| md | `p-4` / 16px | внутренний padding карточки |
| lg | `gap-6` / 24px | между карточками в сетке |
| xl | `py-12` / 48px | между секциями (мобила) |
| 2xl | `py-16..24` | между секциями (десктоп) |

Кратность 4px. Произвольные значения (`p-[13px]`) — не использовать.

---

## Motion

MOTION_INTENSITY=3 → только `:hover`/`:active`, без авто-анимаций.

| Что | Длительность | Easing |
|---|---|---|
| Hover карточки/кнопки | < 100мс | `ease` |
| Дропдауны, поповеры | 150–250мс | `ease-out` |
| Модалки, шторки | 200–500мс | `ease-out` |

Правила (полностью — `docs/core/motion.md`):
- Анимировать **только** `transform` / `opacity`. Не `width/height/top/left/flex-grow`.
- `transition-all` — запрещён, перечислять свойства явно.
- `ease-in` запрещён **везде** — и на входе, и на выходе (линтер даёт ERROR).
- **Выход = 65–75% длительности входа** (вошло за 300мс → ушло за 200мс).
- Уважать `prefers-reduced-motion` (глобально обработан в `index.css`).
- **Раскрытие на неизвестную высоту** (аккордеон) — единственное разрешённое
  исключение из «только transform/opacity»: `grid-template-rows` `0fr → 1fr`,
  200мс вход / 140мс выход. `max-height` с магическим потолком запрещён — он
  обрезает длинные ответы.
- Авто-карусели: пауза на hover/focus + отключение при reduced-motion.
- Карусель баннеров на главной — по референсу клиента, во всю ширину:
  сосед 11% + зазор 5.5% + слайд 67% + зазор + сосед (сумма 100), мобиль
  9% / 16px / 82%. Соседи не масштабируются, только `opacity .6`; стрелки —
  белые кружки 44px по центру зазора (`calc(13.75% − 22px)`); радиус 24px
  (`rounded-banner`), слайды 2:1.
- Lottie-сцены (`.lottie-host`) исключены из глобального сброса `transform`
  под reduced-motion: их конечности стоят на SVG-атрибуте, и CSS `none`
  разбирал персонажа на части. Плеер там просто стоит на первом кадре.

---

## Компоненты — ключевые инварианты

### Шапка (Header) — пилюля

- **Позиция:** `position: fixed; top-[10px]; left-0; right-0; z-50`
- **Контейнер:** `max-w-7xl mx-auto`, `rounded-full`, `px-8`, `h-16` (64px)
- **Фон:** `rgb(119 119 119 / 0.5)` (#777777 на 50%) + `backdrop-blur-[8px]`
  — размытие и фон ТОЛЬКО на контейнер, не на весь экран
- **Тень:** `shadow-md` + `drop-shadow-sm`
- **Состав:** слева-направо: навигация (Собаки / Коты и кошки / Ветаптека),
  по центру логотип 160px (десктоп) / 100px (мобиль), справа иконки
  (Telegram / Поиск / Профиль / Избранное / Корзина)
- **Текст:** белый `text-white`, font-medium, класс `.header-pill-text` —
  `filter: drop-shadow(0 1px 2px rgb(0 0 0 / 0.55))`. Фон #777/50% — решение
  владельца; белый на нём даёт ~2:1, читаемость держится тенью (text-shadow
  на SVG не работает, поэтому именно filter). Пункты — высота 44px.
- **Иконки:** белые, класс `.header-pill-icon` (тот же drop-shadow), тач-таргеты
  44px, на ховер `bg-white/15`. Логотип — `.header-pill-logo` с тенью;
  на планшете (md) 96px, с lg — 160px, иначе перекрывает навигацию.
- **Telegram:** брендовый цвет `#0088cc` — исключение из палитры; внутри пилюли
  залитый круг 32px с белым глифом, на hover инверсия (фон белый, глиф
  `#0088cc`) + сдвиг 1px по диагонали
- **Hover иконок:** движение живёт на самом `<svg>` (классы `.header-ico-*`),
  не на 44px-кнопке — тогда соседи не двигаются. Поиск −12°/1.08, профиль
  −1px/1.06, сердце `icoPop` 200ms, корзина +2px/−6°. 180ms `--ease-out`,
  только `(hover: hover)`
- **Корзина в шапке:** Iconoir `simple-cart` (`CartTrolleyIcon`) — тележка с
  ручкой по референсу клиента; остальной сайт остаётся на `CartIcon`
- **Мегаменю:** белая карточка `rounded-card` с тенью, привязана под свой
  пункт навигации (`absolute left-0 top-full mt-3` внутри пункта)
- **Мобиль:** та же пилюля (h-14, px-4), бургер слева, логотип 100px по центру,
  корзина справа; мобильное меню ниже
- **Отступ контента:** `main` класса `pt-[84px]` чтобы контент не уезжал под пилюлю

---

- **Тач-таргеты:** ≥ 44×44px (`w-11 h-11`). Эталон — сердечко в `ProductCard`.
- **Иконочные кнопки:** обязателен `aria-label`.
- **Две карточки, два языка.** Контентная (шаг инструкции, FAQ, инфо-блок,
  колонка футера) — `bg-white border border-line rounded-card`, тени нет.
  Товарная — та же рамка в покое, но на hover получает глубину:
  `shadow-card` + `border-primary-soft` + `translateY(-2px)`. Требование
  глубины из e-commerce-профиля закрывается ховером, а не постоянной тенью.
- **Рамка ИЛИ подложка, не оба.** Блок на `primary-tint` рамки не получает.
- **Плитки категорий на главной** — исключение по референсу клиента: серая
  заливка `#D9D9D9`, радиус 32px (`CategoryTiles.tsx`).
- **Border-radius:** карточки `rounded-card` (14px), кнопки `rounded-xl`,
  пилюли `rounded-full`. Максимум 16px.
- **Вложенные карточки:** запрещены (карточка в карточке).
- **Цены:** только через `formatPrice` — в БД копейки (`249900` = 2499 ₽).
- **Пустые состояния:** заголовок + пояснение + кнопка next step.
- **Высота экрана:** `min-h-[100dvh]`, не `min-h-screen` (100vh прыгает на мобиле).

### Кнопки (CTA и действия)

- **Главные CTA** (`.btn-primary`): фон `ink` `#232323`, текст белый, радиус 30px,
  высота ≥44px, font-size 13px (12px на мобилях ≤ 767px), font-weight 600.
  На ховер (при наличии мыши) фон темнеет до `#000000` + тень `0 4px 12px rgb(0 0 0 / .18)`.
  Ядро spotlight остаётся (локальное свечение под курсором), ободок нейтральный
  `rgb(255 255 255 / .30)` вместо синего. Нажатие — scale 0.97 + затемнение.
  Disabled = opacity 50%.
- **Вторичные/outline** (`.btn-outline`): белый фон, рамка `line`, текст `navy-900`.
  На ховер фон светлеет до `primary-tint`, рамка меняется на `primary-soft`.
- **Чип/фильтр** (`.btn-press`): фон без рамки, scale 0.94 на нажатие.
- **Ссылки:** текст `primary-hover` `#2E5DC8`, никогда не синий текст на синем фоне.
  Стрелка в тексте сдвигается +3px на ховер.

---

## Решённые вопросы

- **Чёрные CTA вместо синих** (закрыт 10.09.2026 по требованию клиента).
  Все главные кнопки действия переведены на чёрный фон `ink` `#232323` с белым
  текстом (контраст 15.2:1). Синий остаётся для ссылок, чипов, иконок, фокус-ринга.
  Amber — акцент бренда: Scoins, скидки, рейтинг (как текст только в варианте `amber-600`).
